name: Prettier

on:
  workflow_call:

jobs:
  prettier:
    name: Prettier
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: NPM clean install
        run: npm ci

      - name: Run Prettier
        run: npx prettier --check .
