name: ESLint

on:
  workflow_call:

jobs:
  eslint:
    name: ESLint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: NPM clean install
        run: npm ci

      - name: Run ESLint
        run: npx eslint .
