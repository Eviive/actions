name: Prettier
author: Eviive
description: Runs Prettier on the codebase

inputs:
  version:
    description: The Prettier version to install
    default: latest

runs:
  using: composite
  steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Setup Prettier
      shell: bash
      run: npm install --global prettier@${{ inputs.version }}

    - name: Prettier
      shell: bash
      run: prettier --check .
